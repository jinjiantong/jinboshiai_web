# 智能客服链路测试报告

> **测试日期**：2026-06-11  
> **测试人员**：TRAE AI  
> **测试环境**：生产环境

---

## 一、完整链路架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户对话完整链路                            │
└─────────────────────────────────────────────────────────────────┘

用户提问
    │
    ▼
┌─────────────────────────────────────┐
│  1. Redis缓存检查                  │
│  • 检查是否有缓存                  │
│  • 有缓存 → 直接返回              │
│  • 无缓存 → 继续下一步            │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  2. MiniMax Embedding API           │
│  • 生成1536维查询向量              │
│  • 发送到Qdrant                  │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  3. Qdrant向量数据库              │
│  • 语义检索TOP3相关知识            │
│  • 返回分数和内容                 │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  4. MiniMax Chat API              │
│  • 组装Prompt                     │
│  • 生成AI回答                     │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  5. Redis缓存设置                  │
│  • 缓存回答                       │
│  • 过期时间1小时                  │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  6. MySQL数据库                   │
│  • 保存会话                       │
│  • 保存用户消息                   │
│  • 保存AI回复                    │
└─────────────────────────────────────┘
    │
    ▼
返回给用户
```

---

## 二、节点测试结果

### 2.1 节点1：Redis缓存 ✅

| 测试项 | 状态 | 结果 |
|--------|------|------|
| Redis服务连接 | ✅ | 端口6379开放 |
| 首次请求 | ✅ | cached:false（新请求） |
| 二次请求 | ✅ | cached:true（命中缓存） |
| 响应时间提升 | ✅ | 4084ms → 11ms |
| 缓存数据 | ✅ | chat:xxx 存储成功 |

**结论**：Redis缓存功能正常，有效提升响应速度。

---

### 2.2 节点2：MySQL数据库 ✅

| 测试项 | 状态 | 结果 |
|--------|------|------|
| MySQL服务连接 | ✅ | 端口3306开放 |
| 表连接 | ✅ | chat_messages → chat_sessions |
| 数据插入 | ✅ | 用户消息和AI回复存储 |
| 数据查询 | ✅ | 统计功能正常 |

**结论**：MySQL数据库功能正常，持久化存储工作良好。

---

### 2.3 节点3：向量数据库(Qdrant) ✅

| 测试项 | 状态 | 结果 |
|--------|------|------|
| Qdrant服务连接 | ✅ | 端口6333开放 |
| Collection存在 | ✅ | chatbot_knowledge |
| 向量数量 | ✅ | 10条 |
| Embedding生成 | ✅ | 1536维向量 |
| 语义检索 | ✅ | 返回TOP3相关知识 |

**检索结果示例**：

| 知识标题 | 相似度分数 |
|---------|-----------|
| 报名咨询 | 0.32 |
| 第四章：一切皆技能 | 0.31 |
| 第一章：课程简介 | 0.27 |

**结论**：Qdrant向量库功能正常，语义检索工作良好。

---

### 2.4 节点4：大模型(MiniMax) ✅

| 测试项 | 状态 | 结果 |
|--------|------|------|
| Chat API连接 | ✅ | API正常响应 |
| 对话生成 | ✅ | 返回自然语言回答 |
| Embedding API | ✅ | 返回1536维向量 |

**测试结果**：
```
用户输入：你好
AI回复：你好！有什么可以帮助你的吗？
```

**结论**：MiniMax大模型功能正常，对话生成准确。

---

## 三、端到端测试结果

### 3.1 完整对话测试

| 步骤 | 用户问题 | 预期结果 | 实际结果 |
|------|---------|---------|---------|
| 1 | 课程有哪些 | Redis无缓存 → RAG检索 → AI生成 | ✅ cached:false, 成功返回 |
| 2 | 怎么报名 | Redis无缓存 → RAG检索 → AI生成 | ✅ cached:false, 成功返回 |
| 3 | 课程有哪些 | Redis命中缓存 → 直接返回 | ✅ cached:true, 响应11ms |

### 3.2 数据流向验证

```
用户输入："课程有哪些"
    │
    ▼
[Redis] 首次查询 → 缓存未命中
    │
    ▼
[MiniMax] 生成向量 → 1536维
    │
    ▼
[Qdrant] 语义检索 → 返回TOP3相关知识
    │
    ▼
[MiniMax] 生成回答 → AI回复
    │
    ▼
[Redis] 设置缓存 → chat:xxx (1小时)
    │
    ▼
[MySQL] 存储记录 → 会话+消息
    │
    ▼
返回用户
```

---

## 四、性能测试

| 指标 | 首次请求 | 缓存命中 | 提升 |
|------|---------|---------|------|
| 响应时间 | 4084ms | 11ms | **99.7%** |
| API调用 | RAG+Chat | 直接返回 | 减少2次 |

---

## 五、结论

### 5.1 链路完整性

| 节点 | 功能 | 状态 |
|------|------|------|
| Redis缓存 | 提升响应速度 | ✅ 正常 |
| MySQL数据库 | 数据持久化 | ✅ 正常 |
| Qdrant向量库 | 语义检索 | ✅ 正常 |
| MiniMax大模型 | 对话生成 | ✅ 正常 |
| **整体链路** | **端到端** | ✅ **全部通过** |

### 5.2 功能验证

- ✅ Redis缓存减少重复计算
- ✅ MySQL存储对话记录
- ✅ Qdrant检索相关知识
- ✅ MiniMax生成自然语言回答
- ✅ 响应时间大幅优化

### 5.3 建议

1. **监控**：建议添加监控告警
2. **扩容**：高并发时可扩展Redis集群
3. **优化**：可考虑增加知识库条目

---

**测试完成 - 所有节点均正常运行**
